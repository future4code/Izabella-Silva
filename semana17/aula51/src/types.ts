export type user = {
   id: string
   name: string
   nickname: string
   email: string
   address: string
}

export type addressInfo = {
   street: string
   neighborhood: string
   city: string
   state: string
}